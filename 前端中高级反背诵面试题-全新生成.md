# 前端中高级反背诵面试题（全新生成）

## 问题 1：为什么 Agent 要动态查询 Ant Design 图标，而不是把所有图标名写进 prompt？

### 问题

A2UI 生成 Icon 时，模型可能输出 `home`、`HomeOutlined` 或不存在的图标名。项目里引入了 `get_antd_icons` 工具，又在客户端做了 legacy Material 图标名到 Ant Design 图标名的映射。请说明：为什么这不是简单的“补一个枚举表”问题？如果线上出现大量 `?` 图标，你会从模型、服务端工具、客户端渲染三个层面怎么定位？

### 考察点

考察候选人是否理解 LLM 生成约束、工具调用、客户端兼容兜底和组件库运行时映射之间的协作关系。重点是能否区分“模型不知道可用图标”“工具没注入或没调用”“客户端无法 resolve”这三类问题。

### 参考答案

这里不能只靠 prompt 写死所有图标名。图标集合很大，直接塞进 system prompt 会浪费 token，也不利于后续组件库升级。更合理的是让服务端注入一个可查询工具，模型按需查询可用图标，再生成协议里的 `Icon.name.literalString`。

客户端仍然需要兜底，因为模型可能沿用旧 catalog 里的 Material 风格名称，也可能直接生成 Ant Design PascalCase 名称。渲染层先尝试按 Ant Design 导出名查找，再尝试 legacy 映射，最后才降级显示原始文本或 `?`。

如果线上大量出现 `?`，我会先看请求体里的 tools 是否包含 `get_antd_icons`，以及环境变量是否关闭了默认工具注入；再看 LLM 原始输出里 Icon name 是不是工具返回的 PascalCase；最后看客户端图标包是否真的有这个导出名，以及 legacy 映射表是否覆盖了旧名称。

真正的风险是“三套约束不一致”：prompt 说一种名字，工具返回另一种名字，客户端映射又支持第三种名字。高级工程师会考虑把 catalog、工具返回和客户端 resolver 的可用图标源统一，避免靠人工同步。

### 学习路径

- 知识点：LLM tool calling、prompt token 预算、组件枚举约束、客户端兼容兜底。
- 源码模块：`server/a2ui-playground-server/src/agent/defaultA2uiTools.ts`、`executeServerTool.ts`、`antdIcons.ts`。
- 源码模块：`packages/a2ui-react/src/components/Icon.tsx`、`packages/a2ui-react/src/icon/legacyMaterialToAntd.ts`。
- 推荐资料：OpenAI function calling/tool calling 文档、Ant Design Icons 导出机制。

## 问题 2：多模态图片生成 UI 时，为什么前端限制图片数量和大小还不够？

### 问题

Playground 支持用户上传图片，让模型根据图片生成 A2UI。前端限制了图片数量和单张大小，并把图片转成 AG-UI binary part。你认为这个链路还有哪些隐患？如果某些图片上传后模型完全忽略图片，应该怎么定位？

### 考察点

考察候选人是否理解浏览器文件读取、base64 体积膨胀、多模态消息转换、视觉模型选择和服务端兼容网关差异。

### 参考答案

前端限制文件数量和大小只是第一层保护。图片转 base64 后体积会膨胀，请求体、日志、内存和代理层限制都可能成为瓶颈。还要考虑 MIME 识别不可靠、拖入文件 type 为空、超大 data URL 影响序列化和传输、服务端 body parser 限制、以及不同 LLM 供应商对 image_url/data URL 的支持差异。

如果模型忽略图片，我会先看前端发出的 messages content 是否真的是数组，并且包含 `binary`、`mimeType` 和 base64 data。再看服务端是否识别到用户消息里有 image binary，从而选择视觉模型。接着看 AG-UI content part 是否成功转换成 OpenAI compatible 的 `image_url` part。最后看当前 provider/baseURL/model 是否真的支持多模态。

这里的陷阱是：前端预览能看到图片，不代表服务端传给模型的格式正确；服务端选择了一个模型，也不代表这个兼容网关支持 data URL 图片。中高级回答应该能把“上传成功”和“模型看到图片”拆成两件事。

### 学习路径

- 知识点：FileReader、base64 膨胀、MIME 推断、请求体大小、视觉模型输入格式。
- 源码模块：`web/a2ui-playground/src/App.tsx`：图片选择、base64 转换、AG-UI content part 构造。
- 源码模块：`server/a2ui-playground-server/src/agent/agUiContentToOpenAi.ts`：binary 到 OpenAI content part。
- 源码模块：`server/a2ui-playground-server/src/provider/llm.ts`：视觉模型选择。
- 推荐资料：MDN FileReader、OpenAI Vision input 文档、AG-UI message schema。

## 问题 3：LLM provider 配置用了缓存，为什么热切换环境变量可能不生效？

### 问题

项目支持 Kimi、OpenAI 和 LLM 兼容网关，`getOpenAiCompatibleClient` 会缓存客户端。现在本地调试时切换 `.env` 后发现请求仍然打到旧 baseURL。你怎么解释这个现象？如果要做更稳定的 provider 管理，应该怎么改？

### 考察点

考察候选人是否理解服务端运行时配置、单例缓存、开发热重载、provider 优先级和多模型选择策略。

### 参考答案

这是典型的运行时缓存问题。客户端创建时读取了一次环境变量，然后存在模块级缓存里。后面即使 `.env` 文件变了，只要 Node 进程没重启，或者没有清空这个缓存，后续请求仍然复用旧 client。

项目里 provider 选择还有优先级：显式 `LLM_PROVIDER` 优先，否则按 key 存在情况推断；文本模型和视觉模型也分别有默认值。排查时不能只看一个 `OPENAI_API_KEY`，要同时看 provider、baseURL、model、vision model 和 legacy LLM 配置是否混用。

更稳定的设计可以是启动时读取并冻结配置，打印当前 provider 摘要，避免每次请求动态猜；开发模式提供明确的 reset 或重启提示；测试里用 `resetLlmClientCache` 隔离 case；如果未来支持多租户 provider，就不能再用全局单例，而要按配置 key 缓存 client。

陷阱是把它当成“环境变量没生效”。本质上是“进程内配置读取时机”和“客户端缓存生命周期”的问题。

### 学习路径

- 知识点：Node 进程环境变量、模块级缓存、热重载、OpenAI compatible client。
- 源码模块：`server/a2ui-playground-server/src/provider/llm.ts`：`getLlmConfig`、`getDefaultChatModel`、`getVisionChatModel`、`cachedClient`。
- 源码模块：`server/a2ui-playground-server/src/routes/agent.ts`：未配置 client 时 mock fallback。
- 推荐资料：Node.js `process.env`、OpenAI SDK client 配置、dotenv 加载机制。

## 问题 4：Button 同时支持打开链接和本地 dataModel 更新，点击顺序应该怎么设计？

### 问题

A2UI Button 的 action 既可以 `openLink`，也可以 `localDataModelUpdate`。现在一个按钮点击后既要打开外链，又要更新本地 UI 状态。你认为当前设计有哪些边界风险？如果用户反馈点击后页面跳走但状态没更新，应该怎么分析？

### 考察点

考察候选人是否理解浏览器导航副作用、组件 action 语义、本地状态更新、事件处理顺序和安全边界。

### 参考答案

这个 action 不是普通按钮点击，它有两个副作用：导航和本地状态写入。顺序会影响结果。如果 `_self` 导航先发生，当前页面可能很快卸载，本地 dataModel 更新和刷新树就没有机会完成。即使 `_blank`，浏览器也可能受弹窗策略影响。

安全上还要过滤危险 URL，例如 `javascript:`。当前 host 层把 URL 解析成绝对地址并拒绝危险 scheme，这是必要的。但还要考虑 target 默认值、noopener/noreferrer、同页导航前是否需要先提交状态、以及 action 失败时是否需要给用户反馈。

如果反馈“跳走但状态没更新”，我会先看 action 里 target 是 `_self` 还是 `_blank`，再看 onOpenLink 和 onLocalDataModelUpdate 的执行顺序，确认本地更新后有没有触发 tree refresh。更稳的语义可能是把 action 拆成互斥类型，或者定义 action pipeline：先校验、再更新本地状态、最后导航；对 `_self` 导航不承诺页面内状态可见。

另外，组件里遗留 `debugger` 也属于真实工程风险：线上构建如果没有移除，会影响开发调试和用户体验。这类问题能看出候选人是否真的读过代码。

### 学习路径

- 知识点：浏览器导航、`window.open`、URL 安全、React 事件、状态刷新时机。
- 源码模块：`packages/a2ui-react/src/components/Button.tsx`：action 执行顺序。
- 源码模块：`packages/a2ui-react/src/components/index.ts`：`onLocalDataModelUpdate` 注入。
- 源码模块：`web/a2ui-playground/src/App.tsx`：`openExternalLink`、`requestTreeRefresh`。
- 推荐资料：MDN `window.open`、URL API、安全跳转最佳实践。

## 问题 5：协议 styles 直接覆盖组件默认样式，灵活性和一致性怎么权衡？

### 问题

组件库里很多组件通过 `mergeComponentStyles(base, styles)` 让协议 styles 覆盖默认样式。这样模型可以自由控制 UI，但也可能破坏视觉规范。你会如何评价这种设计？如果要上线到真实业务，应该怎么限制 styles？

### 考察点

考察候选人是否理解协议驱动 UI 的样式自由度、设计系统约束、CSS 安全、可维护性和渲染一致性。

### 参考答案

当前策略很适合 playground，因为它给模型足够自由度，能快速验证 UI 生成能力。但真实业务里直接让协议覆盖任意 inline style 风险很高：可能破坏布局、对比度、响应式、品牌规范，也可能出现不可点击、溢出、文字不可读等问题。

更稳的方案不是完全禁止样式，而是把自由度收敛到设计 token 和白名单属性。比如颜色只能引用 token，spacing 只能用固定阶梯，布局只能从少量 variants 里选。对危险属性如 `position: fixed`、极端 z-index、巨大 transform、不可见 opacity 要做校验或拒绝。

还要区分组件语义样式和布局样式。Button 的 variant、size 应该优先通过结构化 props 表达，而不是让模型直接写 background/border。styles 可以作为低优先级 escape hatch，并且必须经过协议校验和可视化回归。

中高级回答要说出权衡：playground 阶段要灵活，生产阶段要治理。否则模型生成 UI 的不可控性会转化为线上一致性和可访问性问题。

### 学习路径

- 知识点：设计 token、CSS inline style、样式白名单、可访问性对比度。
- 源码模块：`packages/a2ui-react/src/components/mergeComponentStyles.ts`。
- 源码模块：`packages/a2ui-react/src/components/Button.tsx`、`Text.tsx`、`Card.tsx`、`Image.tsx`。
- 源码模块：`docs/catalog_definition.json`、`specification/v0_8/json/standard_catalog_definition.json`。
- 推荐资料：Design Tokens Community Group、WCAG contrast、Ant Design token 体系。

## 问题 6：组件入场动画为什么会和 store、timer、重渲染纠缠在一起？

### 问题

`createRenderMap` 会给每个组件包一层 animated class，并在未 mounted 时用 `setTimeout` 标记 mounted。这个设计看起来简单，但为什么可能导致重复 timer、动画错乱或额外渲染？你会如何改得更可控？

### 考察点

考察候选人是否理解 React render 函数副作用、timer 生命周期、store 更新触发渲染、组件 id 稳定性和动画状态管理。

### 参考答案

这里最大的问题是 renderMap 的渲染函数里直接安排 `setTimeout`。渲染函数理论上应该尽量纯，尤其在 React 严格模式或频繁重建 element tree 时，同一个组件可能多次经过 renderMap，于是重复注册 timer。

timer 到点后会写 store 的 mounted 状态，但这次 store 更新本身不一定和 React 组件生命周期严格对应。模板列表如果使用 index 合成 id，插入或重排会让同一个业务项换 id，动画状态也会跟着错乱。

更可控的做法是把动画状态放到真正的 React 组件生命周期里，用 `useEffect` 在 mount 后触发，cleanup 时清理 timer。或者由 runtime 在节点首次进入图时标记动画状态，而不是每次 renderMap 被调用都创建副作用。还要保证 id 稳定，尤其是模板列表。

这个问题的陷阱是候选人只说“setTimeout 300ms 后改状态”。中高级应该能指出副作用放在 render adapter 里不纯，可能被重渲染放大。

### 学习路径

- 知识点：React render purity、StrictMode、timer cleanup、动画状态、稳定 key/id。
- 源码模块：`packages/a2ui-react/src/components/index.ts`：`createRenderMap`、`getHasMounted`、`onMountComplete`。
- 源码模块：`packages/a2ui-core/src/store/index.ts`：`hasMounted`、`setHydrateNodeMounted`。
- 源码模块：`packages/a2ui-core/src/parser/index.ts`：模板 synthetic id、treeBuild。
- 推荐资料：React StrictMode 文档、React effects 文档。

## 问题 7：mock 流能跑通，真实 LLM 流失败，为什么不能说明前端 parser 没问题？

### 问题

本地选择 mock 场景时预览正常，但走真实 LLM `/api/agent` 时经常失败。候选人说“mock 能跑说明前端没问题，是模型问题”。你认同吗？mock 和真实链路有哪些关键差异会影响结论？

### 考察点

考察候选人是否理解测试替身和真实链路的差异，能否判断 mock 覆盖了什么、没覆盖什么。重点是不要把 smoke test 当成完整端到端验证。

### 参考答案

我不认同这个结论。mock 能跑只说明一组固定协议样例能被当前 parser 和 renderer 处理，不代表真实 LLM 链路没问题。

真实链路多了很多变量：模型可能输出 markdown 包裹、多个 JSON 对象、工具调用、非法字段、增量协议、图标名不合法；服务端还会做 JSON 解析、协议合并、拆消息、随机切 chunk；前端还要处理 SSE、事件名、非标准 content-type fallback 和流结束 flush。

mock 通常是“协议已经正确”的理想输入，真实 LLM 是“文本到协议”的不稳定输入。两者定位时要分层：如果 JSONL 已经是合法的但 parser 失败，才偏前端；如果 LLM raw 就不是 JSON，偏模型和 prompt；如果服务端合并后协议坏了，偏 merge/split。

更好的测试应该有三类：固定 mock 验证 renderer，服务端生成的 JSONL golden case 验证 parser，真实或录制的 LLM raw 验证 parse/merge/split。这样才不会用一个 mock 场景覆盖所有风险。

### 学习路径

- 知识点：测试替身、golden test、端到端链路分层、LLM 输出不确定性。
- 源码模块：`web/a2ui-playground/src/mock/stream-simulator.ts`。
- 源码模块：`server/a2ui-playground-server/src/agent/mockStream.ts`、`llmA2uiAgentStream.ts`。
- 源码模块：`server/a2ui-playground-server/src/a2ui/splitCombinedMessage.ts`、`mergeA2uiProtocol.ts`。
- 源码模块：`packages/a2ui-core/mock/*.json`、`packages/a2ui-core/test/*.test.ts`。

## 问题 8：协议 schema 和运行时 parser 不一致时，应该信谁？

### 问题

项目里既有 specification 下的协议 schema/eval，也有 core runtime parser。假设模型生成的协议能通过某个 JSON schema 校验，但运行时渲染异常；或者 runtime 能渲染，但 schema 不认可。你会如何处理这种“不一致”？长期应该怎么治理？

### 考察点

考察候选人是否理解协议驱动系统中的 schema、runtime、prompt、mock 和测试必须共同演进。重点是能否提出治理机制，而不是简单说“改到一致”。

### 参考答案

短期定位要先确认不一致发生在哪一层。schema 负责描述协议合法性，runtime 负责执行协议，两者都可能落后。能过 schema 但 runtime 异常，说明 runtime 不支持某些合法字段，或者 parser 的实现有 bug。runtime 能渲染但 schema 不认可，可能是 runtime 引入了扩展字段但没有同步协议定义。

不能简单说“以代码为准”或“以 schema 为准”。如果这是公开协议或多端协议，schema 应该是契约；runtime 只能实现契约内能力。playground 阶段可以有实验字段，但要标注扩展并进入兼容策略。

长期治理应该让 schema、catalog、prompt、mock、runtime test 串起来。新增字段时必须同步 schema、生成 prompt、mock 示例和 runtime 测试；服务端也应该在下发前做协议校验，失败时把错误返回给调试面板，而不是让客户端静默降级。

高级答案会关注版本。协议 v0.8、客户端 runtime、服务端 prompt 如果没有版本协商，就很容易出现模型按新版生成、客户端按旧版解析的问题。

### 学习路径

- 知识点：JSON Schema、协议版本、契约测试、兼容性策略。
- 源码模块：`specification/v0_8/json/*.json`。
- 源码模块：`specification/v0_8/eval/src/*.ts`。
- 源码模块：`docs/catalog_definition.json`、`docs/a2ui-agent-prompt-v0_8.md`。
- 源码模块：`packages/a2ui-core/src/parser/index.ts`、`packages/a2ui-core/test/*.test.ts`。
- 推荐资料：JSON Schema 官方文档、contract testing、语义化版本兼容策略。

## 问题 9：`import * as AntdIcons` 会带来什么工程风险？

### 问题

Icon 组件通过 `import * as AntdIcons from '@ant-design/icons'` 做运行时动态查找。这个实现很方便，但如果要发布成生产 SDK，你会担心什么？如何在“模型动态图标名”和“bundle 体积”之间做权衡？

### 考察点

考察候选人是否理解动态属性访问对 tree-shaking 的影响、图标包体积、运行时 registry 和协议灵活性的工程权衡。

### 参考答案

`import * as AntdIcons` 最大的好处是运行时可以按字符串动态 resolve 任意图标名，非常适合模型生成。但它可能让打包器无法精确 tree-shaking，导致把大量图标带进产物。对 playground 可以接受，对生产 SDK 就要谨慎。

如果协议允许任意图标名，前端必须有一个大 registry；如果要控制体积，就要收敛可用图标集合。可以做成构建期生成白名单 registry，只导入 catalog 支持的图标；也可以让宿主注入 icon registry；或者把图标渲染改成远程 SVG/字体图标方案。

权衡点是：A2UI 的灵活性来自模型动态生成，但业务端需要可控体积和稳定视觉。一个可落地方案是默认小图标集，加可选扩展包；服务端工具只返回当前宿主注册过的图标，模型不会生成宿主没有的图标。

陷阱是只说“Vite 会 tree-shaking”。动态命名空间访问通常会削弱 tree-shaking，必须看实际 bundle 分析结果。

### 学习路径

- 知识点：ESM tree-shaking、namespace import、动态属性访问、图标 registry。
- 源码模块：`packages/a2ui-react/src/components/Icon.tsx`。
- 源码模块：`server/a2ui-playground-server/src/agent/antdIcons.ts`。
- 源码模块：`server/a2ui-playground-server/src/agent/defaultA2uiTools.ts`。
- 推荐工具：rollup-plugin-visualizer、webpack-bundle-analyzer、Vite build analyze。

## 问题 10：为什么 `/api/agent` 要强制 SSE 文本编码，而不是相信客户端 Accept 协商？

### 问题

服务端 `/api/agent` 明确用 `EventEncoder({ accept: 'text/event-stream' })`，并注释说忽略客户端 protobuf 协商，否则 Content-Type 和正文会不一致。这个细节说明了什么？如果线上接入了代理或网关后流式渲染变成“一次性返回”，你会怎么定位？

### 考察点

考察候选人是否理解 SSE 协议、内容协商、代理缓冲、流式响应头和前端消费逻辑。重点是能否把“协议格式”和“传输行为”分开分析。

### 参考答案

这个细节说明服务端不只是“返回事件”，还必须保证响应头和实际正文编码一致。AG-UI 客户端可能带 protobuf 的 Accept，如果 encoder 按 Accept 协商出 protobuf content-type，但服务端实际写的是 SSE 文本帧，前端或代理就可能按错误格式处理。

所以这里强制 `text/event-stream` 是为了让传输契约稳定：正文就是 `data: {...}\n\n`，前端按 SSE 文本解析。这个选择牺牲了协商灵活性，但换来调试和兼容稳定性。

如果线上流式变成一次性返回，我会先看响应头是否还是 `text/event-stream`，是否有 `Cache-Control: no-cache`、`Connection: keep-alive`、`X-Accel-Buffering: no`。再看代理层是否开启了 buffering、压缩或响应聚合。然后用 curl 或浏览器 network timing 看 chunk 是否逐步到达，区分是服务端 generator 没 yield，还是代理吞了 flush。

前端还要有 fallback：如果 content-type 不是 event-stream，项目会尝试按一次性文本解析 SSE data 行。但 fallback 只能兜底“可读”，不能保证流式体验。高级回答应该能说清：协议格式正确不等于链路支持真正 streaming。

### 学习路径

- 知识点：SSE、HTTP streaming、Content-Type、Accept 协商、代理 buffering。
- 源码模块：`server/a2ui-playground-server/src/routes/agent.ts`：`EventEncoder`、响应头、`Readable.from`。
- 源码模块：`web/a2ui-playground/src/App.tsx`：`consumeAgentSse`、非 event-stream fallback。
- 推荐资料：MDN Server-Sent Events、Nginx `X-Accel-Buffering`、Node.js streams。
